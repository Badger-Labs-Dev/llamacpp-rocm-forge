[project]
name = "llamacpp-rocm-forge"
version = "0.1.0"
description = "Build ROCm + llama.cpp Docker images and benchmark them, currently targeting AMD Radeon AI PRO R9700 (gfx1201)"
requires-python = ">=3.10"
dependencies = [
    "huggingface_hub>=0.25",
]

[tool.uv]
package = false
